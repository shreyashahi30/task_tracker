{
  "name": "task-tracker-frontend",
  "version": "1.0.0",
  "description": "A frontend React app for task tracking",
  "main": "index.js",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "dependencies": {
    "axios": "^1.2.0",               // For making API calls
    "react": "^18.0.0",              // Core React library
    "react-dom": "^18.0.0",          // React DOM for web applications
    "react-router-dom": "^6.0.0",    // For navigation and routing
    "react-scripts": "5.0.1"         // Development scripts for React
  },
  "devDependencies": {
    "eslint": "^8.0.0",              // Linting for development
    "eslint-plugin-react": "^7.0.0"  // React specific linting rules
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "author": "Your Name",
  "license": "MIT"
}
